# Training checkpoints and weights
checkpoints/
*.pth
*.pth.tar
*.pt
*.ckpt
*.tar

# Python cache / bytecode
__pycache__/
**/__pycache__/
*.py[cod]

# Datasets (download separately)
# Keep `data/download_data.py` tracked; ignore only the large dataset payloads.
data/coco/**
data/coco-o/**
data/objects365/**
!data/.gitkeep
!data/coco/.gitkeep
!data/coco-o/.gitkeep
!data/objects365/.gitkeep

# Training outputs
output/

# Vendored timm / PyTorch Image Models tree (large; clone separately)
pytorch-image-models/
